import controls from './controls';
import operators from './operator';
import datas from './data';
import mcblocks from './mcblocks';

const converters = Object.assign({}, controls, operators, datas, mcblocks );

export default converters